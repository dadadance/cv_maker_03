# Scripts Catalog

> Index of all permanent scripts in this project. AI agents MUST consult this before creating new scripts.

---

## CLI Commands (`cli/`)

| Command | Description | Usage |
|---------|-------------|-------|
| `main.py` | Main CLI entry point | `uv run python -m cli.main --help` |

---

## Data Processing (`scripts/data/`)

| Script | Description | Usage |
|--------|-------------|-------|
| — | No scripts yet | — |

---

## Migration (`scripts/migration/`)

| Script | Description | Usage |
|--------|-------------|-------|
| — | No scripts yet | — |

---

## Notes

- **Temp scripts:** Use `scripts/temp/` for one-off or WIP scripts (gitignored)
- **Promote if reusable:** Move useful temp scripts to permanent location + update this catalog
